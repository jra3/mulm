//- Alert box error message mixin
//- Displays form-level or general error messages in a styled alert box
//-
//- @param {string} errorKey - The error key to look up in the errors map (e.g., 'form')
//- @param {string} [customClass=''] - Optional additional CSS classes
//-
//- Example usage:
//-   +errorAlert('form')
//-   +errorAlert('upload', 'mt-4')

mixin errorAlert(errorKey, customClass='')
  if errors && errors.has(errorKey)
    .bg-red-50.border.border-red-200.text-red-800.px-4.py-3.rounded.mb-3(
      role="alert"
      aria-live="polite"
      class=customClass
    )= errors.get(errorKey)
