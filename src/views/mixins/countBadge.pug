//- Count Badge Mixin
//- Displays a number in a round red badge
//-
//- @param {number} count - The number to display
//-
//- Usage:
//-   +countBadge(5)

mixin countBadge(count)
	if count && count > 0
		span.inline-flex.items-center.justify-center.bg-red-500.text-white.text-xs.font-bold.rounded-full.min-w-6.w-6.h-6= count
