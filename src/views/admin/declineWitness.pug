aside.fixed.inset-0.bg-black.bg-opacity-50.flex.items-center.justify-center.z-50(
	hx-on:click="if (event.target === this) this.remove()"
)
	div.bg-white.rounded-lg.shadow-xl.max-w-md.w-full.mx-4.p-6(hx-on:click="event.stopPropagation()")
		div.flex.justify-between.items-center.mb-4
			h3.text-lg.font-semibold.text-gray-900 Request More Proof
			button(
				type="button"
				class="text-gray-400 hover:text-gray-600"
				hx-on:click="this.closest('aside').remove()"
				aria-label="Close dialog"
			)
				svg.w-6.h-6(fill="none" stroke="currentColor" viewBox="0 0 24 24")
					path(stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12")

		p.text-gray-600.mb-4 
			| Provide feedback to the member about what additional proof or clarification is needed to verify this 
			= submission.species_type === 'Plant' ? 'propagation' : 'spawn'
			| .

		form(hx-post=`/admin/decline-witness/${submission.id}` hx-target="closest aside" hx-swap="outerHTML")
			div.mb-4
				label.block.text-sm.font-medium.text-gray-700.mb-2(for="reason") Reason for requesting more proof:
				textarea.w-full.px-3.py-2.border.border-gray-300.rounded-md.focus:outline-none.focus:ring-2.focus:ring-blue-500.focus:border-transparent(
					id="reason"
					name="reason"
					rows="4"
					placeholder="Please provide clearer photos of the spawn/fry, additional documentation of the reproduction date, etc."
					required
				)

			div.flex.justify-end.space-x-3
				button(
					type="button"
					class="px-4 py-2 text-sm font-medium text-gray-700 bg-gray-100 border border-gray-300 rounded-md hover:bg-gray-200"
					hx-on:click="this.closest('aside').remove()"
				) Cancel
				button(
					type="submit"
					class="px-4 py-2 text-sm font-medium text-white bg-orange-600 border border-transparent rounded-md hover:bg-orange-700"
				) Send Request